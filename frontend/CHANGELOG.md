# Changelog

## [Unreleased]
### Added
- Added the new Swaya.me logo `logo.png` to `frontend/src/assets/`.

### Changed
- Updating frontend components (`Home`, `App`, `Login`, `Register`, etc.) to use the newly generated logo image instead of the `RocketOutlined` icon.
